import './Navbar.css';

const Navbar = () => {
    return ( <AppBar>
        <Toolbar>
            <Button>
                
            </Button>
        </Toolbar>
    </AppBar>);
}

export default Navbar;